<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.mapper.MemberMapper">

	<insert id="insertMember" parameterType="Member">
		INSERT INTO member (name, email, password, phone, department, address_no, roadname_addr, detail_addr )
		VALUES ( #{ name }, #{ email }, #{ password }, #{ phone }, #{ department }, #{ addressNo }, #{ roadnameAddr }, #{ detailAddr } )
	</insert>
	
	<select id="selectMemberByEmailAndPassword" parameterType="Member" resultType="Member">
		SELECT * FROM member WHERE email = #{ email } AND password = #{ password } AND deleted = '0'
	</select>
	
	<select id="selectMemberByEmail" parameterType="String" resultType="Member">
		SELECT * FROM member WHERE email = #{ email }
	</select>



</mapper>